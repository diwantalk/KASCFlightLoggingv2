namespace KASCFlightLogging.Models
{
    public enum FlightStatus
    {
        Draft,
        Approved,
        Rejected,
        Completed,
        Cancelled
    }

   

    public enum FieldType
    {
        Text,
        Number,
        Time,
        DateTime
    }
}